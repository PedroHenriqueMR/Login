body {
    background-color: #48494963;
}

div {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: flex-start;
}

.title {
    font-family: mon;
    font-size: 3rem;
    font-weight: 300;
    position: relative;
    box-shadow: 1px 1px 10px,
    2px 2px 10px,
    3px 3px 10px,
    4px 4px 10px,
    5px 5px 10px,
    6px 6px 10px;
}

.title::after, 
.title::before{
    position: absolute;
    top: 0%;
    left: 0;
    right: 0%;
    bottom: 0%;
    content: '  ';
}

.title::before{
    background-color: rgb(51, 51, 51);
    animation: escrita steps(11) 1s forwards;
}

@keyframes escrita {
    to{
        left: 100%;
    }
}

.title::after{
    width: 0.35rem;
    background: rgb(#df0000);
    animation: escrita steps(11) 1s forwards;
}

@keyframes piscar{
    to{
        background: transparent;
    }
}

table {
    width: 40%;
    height: 50vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Times New Roman', Times, serif;
    box-shadow:3px 1px 50px,
    4px 2px 50px,
    5px 3px 50px,
    6px 4px 50px,
    7px 5px 50px, 
    8px 6px 50px;
}

.button {
    background-color: #2e2c2c; /* Green */
    border: none;
    color: white;
    padding: 1px 1px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 10px;
    transition-duration: 0.4s;
    cursor: pointer;
}

.button5 {
    background-color: white;
    color: black;
    border: 2px solid #555555;
    
  }
  
  .button5:hover {
    background-color: #555555;
    color: white;
  }

.top {
    padding-top: 80px;
}